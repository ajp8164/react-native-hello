# `@react-native-hello/common`

Types shared across all @react-native-hello/\* packages.

## Installation

Open a Terminal in your project's folder and run,

```sh
npm add @react-native-hello/common
```
